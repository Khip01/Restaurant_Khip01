import 'package:flutter/material.dart';

class UpdateMenu extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(child: Text("Hello Update Menu")),
    );
  }
}
