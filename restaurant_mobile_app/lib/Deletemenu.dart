import 'package:flutter/material.dart';

class DeleteMenu extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
      child: Center(child: Text("Hello Delete Menu")),
    );
  }
}
